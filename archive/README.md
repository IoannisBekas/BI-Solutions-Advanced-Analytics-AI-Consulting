# Local archive

This directory is for retained operational artifacts whose purpose is known but which should not be committed or deployed.

Its contents are ignored by Git. During the August 2026 repository cleanup, two generated Bonusaki QR pilot batches were moved to `archive/local-outputs/bonusaki-qr` rather than deleted.
